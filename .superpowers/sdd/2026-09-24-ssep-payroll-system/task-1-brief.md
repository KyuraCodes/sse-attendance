# Task 1: Repository Initialization and Git Setup

## Task Description
Initialize the Git repository with appropriate ignore rules and create the project README.

## Files
- Create: `.gitignore`
- Create: `README.md`

## Steps
1. Create `.gitignore` containing rules for Node.js, Next.js, Java Maven, IDEs, and environment variables.
2. Initialize git repository (`git init`).
3. Create `README.md` introducing the SSEP Payroll Management System.
4. Stage `.gitignore`, `README.md`, `prd.md`, `.agents/`, and commit with message: `chore: initialize repository and add specifications`.

## Constraints
- ZERO em-dash (`—`) allowed. Use regular hyphen `-` only.
- Working directory: `d:\Files Ammar\coding\website\Attendance`.
